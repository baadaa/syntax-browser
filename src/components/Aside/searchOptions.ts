export const fuseOption = {
  isCaseSensitive: false,
  includeMatches: true,
  shouldSort: true,
  minMatchCharLength: 2,
  threshold: 0.6,
  ignoreLocation: true,
  findAllMatches: true,
};

export const MIN_QUERY_LENGTH = 4;
